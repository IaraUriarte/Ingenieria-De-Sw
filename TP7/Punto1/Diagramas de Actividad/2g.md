
```mermaid
flowchart TD
    Start((*)) --> A["Get(ticket.ID)"]
    A --> D1{Resultado}
    D1 -->|Ticket| B["Set(ticket.ID, descripción)"]
    D1 -->|Error| E1[Retorna error]
    B --> C["New(ticket.ID, descripción)"]
    C --> D2{Resultado}
    D2 -->|LogAuditoria.ID| F["Status(estado)"]
    D2 -->|Error| E2[Retorna error]
    F --> D3{Resultado}
    D3 -->|OK| G[Retorna OK]
    D3 -->|Error| E3[Retorna error]
    G --> End((*))
    E1 --> End
    E2 --> End
    E3 --> End
```
